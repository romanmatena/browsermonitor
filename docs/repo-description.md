# Repo description (GitHub / Git server)

**Links:** [npm](https://www.npmjs.com/package/browsermonitor) · [GitHub](https://github.com/romanmatena/browsermonitor)

Short description for the repository (About / description field):

**One-line:**
```
Browser console and network monitoring for debugging and LLM workflows. Capture DOM, console, screenshot, and requests via Puppeteer; HTTP API for dump, status, clear, and tab switch.
```

**Alternative (shorter):**
```
Capture browser console, network, DOM, and screenshot for debugging. Puppeteer-based; HTTP API for LLM/scripts. Open, join, or interactive mode.
```
